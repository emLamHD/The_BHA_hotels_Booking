# 2026-09-11 — Plan

## Bối cảnh

`PMS-CAL-001.1` đã merge và đóng (PR #41, merge commit
`e0f5a395aec15cc02e328433a97850e30e165675`, `2026-09-03T03:11:21Z`). PR #42
(`fix(governance): reconcile merged PR state`) đã bị đóng **không merge**, nên
không có nội dung nào của nó nằm trong `develop`.

Owner thay kế hoạch một PR lớn cho `PMS-CAL-001.2` bằng các **checkpoint độc
lập**, mỗi checkpoint một PR nhỏ. Chỉ `CP01` được kích hoạt trong phiên này;
`CP02` và mọi mutation nghiệp vụ khác chưa kích hoạt.

## Work item hôm nay

`PMS-CAL-001.2-CP01` — Local Admin Write Gate foundation.

- `IMPLEMENTER: CLAUDE`, `REVIEWER: CODEX_READ_ONLY`
- Baseline `e0f5a395aec15cc02e328433a97850e30e165675`
- Feature branch `feature/pms-cal-001-2-cp01-local-write-gate`

## Phạm vi dự kiến

1. `AdminCalendar:EnableUnauthenticatedWrite` — opt-in riêng, mặc định `false`,
   độc lập với read flag, Production startup-fatal.
2. `AdminCalendarWriteGateFilter` — resource filter riêng cho write: HTTPS +
   Development → loopback hai đầu → không có `Forwarded`/`X-Forwarded-*` → write
   flag (404); đúng một `Origin` khớp `Cors:AdminOrigins` (403);
   `application/json` (415).
3. CORS policy `admin-calendar-write` (POST, `Content-Type`, không credentials)
   và hợp đồng composition cho route ghi **tương lai**.
4. Chốt composition bằng test-only controller trong test assembly; **không**
   thêm endpoint nào vào production route surface.
5. README local setup; SNAPSHOT/AGENTS factual correction; completion report.

## Ngoài phạm vi

Frontend, `canWrite` DTO, assignment/block endpoint, audit actor/evidence
service, application/persistence/domain behavior, schema/migration, Customer API
contract, auth/RBAC framework, dependencies, `.github/workflows`, governance
docs, `CP02`.

## First action

Preflight: xác minh repository root, `origin/develop` == baseline, working tree
sạch, không có PR trùng scope; rồi `git switch -c` feature branch từ baseline.

---

## Work item thứ hai trong ngày — `PMS-CAL-001.2-CP02`

`PMS-CAL-001.2-CP02` — Admin Reservation Assignment Create API. Work item/PR
độc lập sau khi PR #43 (`CP01`) đã merge.

- `IMPLEMENTER: CLAUDE`, `REVIEWER: CODEX_READ_ONLY`
- Baseline `c8c1ea50e2b2eec44b1ae30e65a7919596458f99` (== `origin/develop`)
- Feature branch `feature/pms-cal-001-2-cp02-assignment-create-api`

## Phạm vi dự kiến (CP02)

Đúng một endpoint HTTP local-only:
`POST /api/admin/v1/properties/{propertyId:guid}/reservation-assignments`, là
adapter mỏng gọi `IAssignmentMutationStore.CreateAsync` đã có. Composition
`CP01`: `[EnableCors("admin-calendar-write")]`, `[IgnoreAntiforgeryToken]`,
`[ServiceFilter(AdminCalendarWriteGateFilter)]`, không `[Consumes]`. Actor và
authorization evidence do server sở hữu, cố định, không phải danh tính nhân
viên. Map `Succeeded/Invalid/Unauthorized/NotFound/Conflict` →
`201/400/403/404/409`.

## Ngoài phạm vi (CP02)

Move/unassign/split/batch, operational block, frontend, auth/RBAC,
idempotency, schema/migration, `Program.cs`, `AdminCalendarWriteGateFilter`,
`CP03`.

## Correction `PMS-CAL-001.2-CP02-C1`

OC `CORRECTION_AUTHORIZED`: không tách CP02, không đổi production code. Thu
hẹp hai assertion test đã merge về đúng phạm vi sở hữu của chúng, rồi hoàn tất
state docs, chạy full suite, push và mở Draft PR.

## Correction `PMS-CAL-001.2-CP02-C2`

OC xác nhận hai finding `[P2]` của Codex. Bắt buộc bốn property JSON bắt buộc
có mặt, và chặn liệt kê khoảng ngày không giới hạn ngay trong store.

## Correction `PMS-CAL-001.2-CP02-C3`

OC xác nhận hai finding `[P2]`: metadata response chưa phản ánh đúng những gì
caller thực sự nhận, và các nguồn CURRENT còn mâu thuẫn với endpoint mới.
Metadata + tài liệu, không đổi runtime.

## Correction `PMS-CAL-001.2-CP02-C4` + `C4-A1`

Owner nhận blocker của C4 là hợp lệ và chọn phương án (a): một `IOperationFilter`
đăng ký trong `AddSwaggerGen`, thu hẹp request content map của đúng operation đó.

## Correction `PMS-CAL-001.2-CP02-C5`

OC xác nhận `[P2]`: cleartext Admin write bị redirect trước gate. Sửa thứ tự
pipeline trong PR này, không tách CP03.
