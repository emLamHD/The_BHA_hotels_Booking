# 2026-09-11 — Worklog

Work item: `PMS-CAL-001.2-CP01` — Local Admin Write Gate foundation.
`IMPLEMENTER: CLAUDE`, `REVIEWER: CODEX_READ_ONLY`.

Evidence canonical: `docs/reports/PMS-CAL-001.2-CP01-completion.md`. Không lặp
lại chi tiết ở đây.

## Preflight

- Repository root `/home/admin1/The_BHA_hotels_Booking`, đúng `REPOSITORY` của
  prompt; `origin` = `emLamHD/The_BHA_hotels_Booking`.
- `git fetch --prune origin`; `origin/develop` =
  `e0f5a395aec15cc02e328433a97850e30e165675` = `BASELINE_SHA`. Working tree
  sạch. Không có PR nào đang mở (`gh pr list --state open` = `[]`), nên không
  có implementation trùng scope.
- PR #42 đã `CLOSED` không merge (head `chore/ai-ops-gov-004-reconcile-pr41`);
  branch local đó và các commit của nó **không** được dùng và không đụng tới.
- Hai linked worktree cũ, không liên quan, thuộc công cụ đã ngưng dùng
  (`/home/admin1/orca/workspaces/…`) tồn tại vật lý trên máy: chỉ ghi nhận
  (audit) theo `docs/governance/RULES.md` §5, **không** dùng, không sửa, không
  xóa. Execution chỉ diễn ra trong primary checkout.
- `git switch -c feature/pms-cal-001-2-cp01-local-write-gate` từ đúng baseline.

## Thực thi

1. `AdminCalendarOptions.EnableUnauthenticatedWrite` (default `false`, độc lập
   với read flag) + Production startup guard riêng trong `Program.cs`.
2. `AdminCalendarWriteGateFilter` — resource filter, đăng ký DI, không global,
   không gắn vào read/Customer route. Thứ tự kiểm tra và mã lỗi: xem report §3.
3. CORS policy `admin-calendar-write` (POST, `Content-Type`, không
   credentials); các policy hiện có không đổi.
4. Test-only probe controller trong test assembly + acceptance matrix hosted
   (156 case) + direct-filter matrix (75 case).
5. README local setup; SNAPSHOT factual correction; AGENTS.md §1 factual fix.

## Kiểm chứng

- `dotnet restore` / `dotnet build --configuration Release` / `dotnet test
  --configuration Release --no-build` với PostgreSQL thật: **244 unit + 500
  integration passed, 0 failed**. `git diff --check` sạch.
- Diff xác nhận không đụng schema/migration/model snapshot, frontend,
  `.github/workflows`, hay production API route surface.

## Một sự cố đáng ghi lại

Lần chạy full suite đầu tiên fail 58 test với `IOException: The configured user
limit (128) on the number of inotify instances has been reached`, ném từ
`WebApplication.CreateBuilder` — không phải từ thứ đang được test. Mỗi
`WebApplicationFactory` build đặt một inotify watch lên appsettings
(`reloadOnChange` là mặc định của host), và ma trận boundary của checkpoint này
đẩy suite vượt giới hạn 128 của Linux — cũng đúng là mặc định của GitHub Actions
runner, nên lỗi này sẽ xảy ra trên CI y như trên máy local.

Sửa trong test code, không đụng cấu hình máy: `PostgreSqlWebApplicationFactory`
đặt `hostBuilder:reloadConfigOnChange=false` (không test nào phụ thuộc vào việc
đọc lại *file* cấu hình; các case "đổi giá trị sau startup" cố ý rebind bound
options), và các case dùng chung một host configuration nay chạy trên một host
thay vì mỗi case một host. Suite xanh sau cả hai thay đổi.

## Correction C1 (`PMS-CAL-001.2-CP01-C1`)

Owner invoke Codex review trên `0ea5e3d`, base `origin/develop`: 3 finding
`[P2]`, không có `[P1]`. OC kết luận cả ba hợp lệ và phát correction prompt.
Cả ba đã sửa; chi tiết canonical ở completion report §10.

1. **Write opt-in không thực sự startup-only.** Filter đọc
   `IOptions<AdminCalendarOptions>.Value` mỗi request; `IOptions<T>` bind lazy,
   nên một host Development khởi động với flag **tắt** vẫn có thể bị mở bởi một
   reloadable configuration source trước request đầu tiên. Thứ tự
   environment-first chỉ che được Production/Staging — không che được
   Development, đúng môi trường gate này chạy. Sửa: `Program.cs` truyền
   `bool` đã freeze lúc startup; filter không nhận `IOptions`/config nào.
   **Red/green:** tạm khôi phục đăng ký `IOptions` để chạy regression test —
   tái hiện đúng exploit, request trả **204 và action đã chạy**; sau khi khôi
   phục code đã sửa: 404, spy = 0.
2. **Claim "closed route không phân biệt được với absent route" là sai.** Đo
   thực tế: closed-gate 404 gửi tới approved Admin origin **có**
   `Access-Control-Allow-Origin: https://localhost:3001`, vì CORS header do
   endpoint metadata quyết định, ngoài filter. Theo quyết định OC: không thêm
   middleware/strip header/CORS tùy biến để giấu route existence. Gỡ mọi phát
   biểu sai trong filter docs, test helper và report; thêm hosted test khẳng
   định header đó **là sự thật**, đồng thời POST vẫn bị từ chối và action không
   chạy, còn origin không được duyệt thì không nhận header nào.
3. **Charset rộng hơn boundary phía sau.** `Encoding.GetEncoding` chấp nhận cả
   `us-ascii`, trong khi System.Text.Json input formatter sau đó vẫn trả 415.
   Sửa: chỉ chấp nhận `application/json` không tham số, hoặc đúng một
   `charset=utf-8`; mọi encoding khác, charset lặp, charset rỗng và tham số
   khác đều 415 tại gate.

Scope C1: filter, `AdminCalendarOptions`, `Program.cs`, hai file test write
gate, README, completion report, worklog này. Không thêm file mới, không đụng
probe controller, read gate, schema, route, dependency hay governance.

## Trạng thái cuối

Dừng mọi thao tác ghi tại checkpoint ổn định, Draft PR mở vào `develop`.
`READY_FOR_CODEX_REVIEW` — chỉ Owner invoke review. `CP02` chưa bắt đầu.

---

# `PMS-CAL-001.2-CP02` — Admin Reservation Assignment Create API

Preflight trên checkout chính: `origin/develop` == baseline
`c8c1ea50e2b2eec44b1ae30e65a7919596458f99`, PR #43 merged tại đúng commit đó,
branch `CP01` đã bị xóa local/remote, working tree sạch, không có PR đang mở.
Branch `feature/pms-cal-001-2-cp02-assignment-create-api` tạo trực tiếp trong
checkout đó bằng `git switch -c`. (Audit: hai linked worktree Orca cũ vẫn tồn
tại vật lý trên máy từ công cụ đã ngưng dùng 2026-08-07; ghi nhận, không dùng,
không sửa — `RULES.md` §5.)

## Đã thực hiện

`AdminReservationAssignmentsController` (147 dòng) — adapter mỏng cho
`IAssignmentMutationStore.CreateAsync`, không thêm logic scheduling nào. Request
DTO cố ý hẹp hơn `CreateAssignmentCommand`: không có `ActorReference`, không có
`AuthorizationEvidence`; body giả mạo hai trường đó bị bỏ qua hoàn toàn
(System.Text.Json mặc định bỏ qua property lạ). Audit ghi hằng số do server sở
hữu — `admin-calendar-local-development`, và
`local-development-write-gate:cross-room-type-confirmed` chỉ khi
`confirmCrossRoomType=true`; đây là mô tả ranh giới ghi local tạm thời, không
phải nhân viên, không phải phê duyệt quản lý, không phải authorization thật.

`AdminCalendarAssignmentApiTests` (447 dòng, 5 test method / 8 case) chỉ chứng
minh phần mới — adapter HTTP: happy path same-RoomType (201 + audit + board đọc
lại thấy đúng segment/version), closed gate 404 trước controller, cross-RoomType
403/403/201 và audit chỉ mang giá trị server, map 400/404/404/409, và OpenAPI
công bố đúng một route POST không lộ trường actor/evidence. Không lặp ma trận
của `AssignmentMutationStoreTests` hay `AdminCalendarWriteGateApiTests`.

## `BLOCKED` — ba assertion đã merge phủ định chính endpoint của CP02

Suite đầy đủ trên PostgreSQL 17 thật: 244/244 unit pass, 517/520 integration
pass. Ba fail **không** nằm trong code CP02 và không phải defect của CP02 — là
ba assertion đã merge khẳng định *sự vắng mặt* của đúng thứ CP02 được giao thêm:

1. `AdminCalendarWriteGateApiTests.cs:1086` — mọi path `/api/admin/**` không
   được có `post`.
2. `AdminCalendarWriteGateApiTests.cs:1106` — không controller nào trong
   `TheBha.Api` được áp `AdminCalendarWriteGateFilter`.
3. `BookingPersistenceTests.cs:622` — allowlist tuyệt đối các OpenAPI path
   chứa "reservation", không có `.../reservation-assignments`.

Cả ba file đều ngoài `ALLOWED_FILES` của CP02, và mục 1–2 nằm trong
`FORBIDDEN_FILES` ("all CP01 tests ... no correction is authorized"). Acceptance
7 (suite xanh) do đó mâu thuẫn trực tiếp với acceptance 5 (OpenAPI có route POST
mới) dưới allowlist hiện tại. Dừng ghi, không sửa file ngoài scope, không push,
không mở PR; chờ OC quyết định (correction prompt mở rộng allowlist cho đúng ba
assertion này, hoặc tách checkpoint).

## Correction C1 (`PMS-CAL-001.2-CP02-C1`)

OC kết luận ba fail không do endpoint gây ra: chúng là assertion đúng cho CP01
— checkpoint mà acceptance là *không có* route ghi nghiệp vụ nào — và hết hiệu
lực đúng lúc CP02 được authorize thêm route đó. `CORRECTION_AUTHORIZED`: sửa
quyền sở hữu test, không đụng production code, không tách checkpoint.

1. `AdminCalendarWriteGateApiTests` — giữ phần chứng minh probe route vắng mặt
   trên host thường và không rò vào Swagger; bỏ vòng lặp cấm mọi
   `POST/PUT/PATCH/DELETE` dưới `/api/admin/`, vì nó biến một test CP01 thành
   sổ đăng ký vĩnh viễn cho mọi route mutation Admin sau này. Đổi tên thành
   `The_ordinary_host_neither_serves_nor_publishes_the_test_only_probe_route`.
   Xóa `The_api_project_contains_no_controller_that_applies_the_write_gate` —
   áp gate lên controller nghiệp vụ đã authorize nay chính là mục đích của
   gate; không thay bằng allowlist controller vì các checkpoint mutation sau
   sẽ làm nó stale lần nữa. Ma trận transport/origin/charset giữ nguyên.
2. `BookingPersistenceTests.OpenApi_exposes_only_the_approved_hold_and_reservation_lifecycle_paths`
   — thêm bộ lọc tiền tố `/api/v1/`, bỏ route Admin Reservation Board khỏi
   allowlist và bỏ hai assertion GET/POST của nó. Test này sở hữu Customer API
   surface; hợp đồng từng route Admin thuộc suite của chính nó, nên route
   reservation-assignment cũng không được thêm vào đây.
3. State docs: `AGENTS.md`, `SNAPSHOT.md` (CP01 merged tại `c8c1ea5`, CP02
   active), plan và worklog này. Không tạo completion-report file.

Không file production nào thay đổi sau `3b149ac`; controller và
`AdminCalendarAssignmentApiTests` đóng băng, C1 không thêm test method nào.
