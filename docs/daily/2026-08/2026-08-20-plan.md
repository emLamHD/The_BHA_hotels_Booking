# THE BHA — PLAN 2026-08-20

## 1. Current state

- `PMS-DATA-DOCS-001` đã hoàn tất phía Claude tại một checkpoint ổn định
  trên branch `docs/pms-data-001-core-database-blueprint-v2`, base
  `origin/develop` tại `f97c3529fb94c08fafad0da059ec1cf2b839b0d0`. Xem
  completion report và `docs/daily/2026-08/2026-08-19-worklog.md` cho chi
  tiết chính xác (commit HEAD, Draft PR URL, checks).
- Kiến trúc PMS/database TARGET đã được Owner duyệt và ghi lại đầy đủ trong
  `docs/design/PMS-DATA-001-core-database-blueprint-v2.md`, ADR 0005, ADR
  0006. Không có phần nào trong đó đã được implement.
- `DATA-001.2` tiếp tục dormant/deferred.
- Không có product implementation work item nào đang active.

## 2. Today's objective

Đưa `PMS-DATA-DOCS-001` qua đúng một lượt review/decision gate — không mở
bất kỳ implementation work item nào.

## 3. Execution order

1. Nếu chưa thực hiện, Owner invoke đúng một lượt review read-only:
   `/codex:review --base origin/develop`.
2. Owner chuyển report của Claude (kèm kết quả Codex khi đã có) cho OC.
3. OC review report/diff/PR/Codex findings, trả về `PASS — recommend Owner
   merge`, `CORRECTION_REQUIRED` kèm correction prompt, hoặc `BLOCKED` kèm
   escalation.
4. Nếu `CORRECTION_REQUIRED`: Owner activate Claude qua đúng một correction
   prompt từ OC cho phần cần sửa; Claude không tự sửa finding nào nếu chưa
   có prompt này.
5. Owner, và chỉ Owner, quyết định Ready/merge/branch cleanup cho
   `PMS-DATA-DOCS-001`.
6. **Sau khi `PMS-DATA-DOCS-001` merge**, Control Tower và Owner, trong một
   quyết định cấp cao riêng biệt, chọn hoặc tiếp tục hoãn work item sản
   phẩm kế tiếp — không có work item nào tự động mở từ việc merge tài liệu
   này.

## 4. Hard gates

- Database migration cho bất kỳ TARGET entity nào trong PMS blueprint
  (Organization, InventoryHold/Item/Night, ReservationUnit/Night,
  RoomOccupancySegment, RoomBlock, FolioEntries, Stay Declaration, OTA
  inbox/outbox): **locked**, không tự động mở.
- Admin Calendar/PMS UI implementation: **locked**.
- OTA adapter design/implementation: **locked**.
- Customer Web change dựa trên PMS blueprint: **locked**.
- `DATA-001.2`: tiếp tục dormant/deferred, không tự động kích hoạt.
- Chỉ Owner được invoke Codex review, merge, và xóa branch.
- Không agent nào tự bắt đầu task kế tiếp từ việc đóng tài liệu sync này.

## 5. Main risks

- Coi việc `PMS-DATA-DOCS-001` merge là authorization ngầm cho một trong các
  module bị khóa ở §4.
- Nhầm CURRENT (BE-003 single-RoomType, đã hoạt động) với TARGET (PMS
  multi-RoomType, chưa implement) khi đọc bất kỳ tài liệu nào đã sync.
  Đọc lại `docs/design/PMS-DATA-001-core-database-blueprint-v2.md` §1 và §2
  nếu có nghi ngờ.
- Codex được cấp nhầm write mode hoặc dùng rescue/transfer trong lượt review
  này.
- Bắt đầu implementation trước khi có correction prompt rõ ràng từ OC, nếu
  review trả `CORRECTION_REQUIRED`.

## 6. Required records

Không tạo file tài liệu mới trong ngày 2026-08-20 trừ khi Owner/Control
Tower phát một Master Execution Prompt mới cho work item kế tiếp. Kết quả
review/decision của `PMS-DATA-DOCS-001` được ghi vào completion report và,
nếu cần, một worklog riêng cho ngày review thực tế diễn ra.

## 7. First action

Xác nhận với Owner liệu `/codex:review --base origin/develop` cho
`PMS-DATA-DOCS-001` đã được invoke hay chưa; nếu chưa, đó là first action
của ngày.
