# THE BHA — PLAN 2026-08-09

## 1. Current state

- Repository revalidation đã hoàn tất; đây không còn là first action của ngày.
- Verified post-PR-#25 integration baseline: `d1008e19ae47617e449493b10f2c613342f48c76`. Đây là baseline đã được xác minh cho tài liệu ngày 2026-08-09, không phải cam kết rằng SHA này sẽ còn là `develop` HEAD sau các commit tài liệu hoặc merge tiếp theo.
- PR #25, `AI-OPS-GOV-002`, đã được merge vào `develop`.
- Lượt Codex review cuối cùng của PR #25 (Owner-invoked) đã chạy trên branch diff thật và không trả actionable finding.
- `AI-OPS-GOV-002`: `PASS`.
- `AI-OPS-DRYRUN-001`: lượt thử đầu tiên bị `BLOCKED` vì không có branch diff hữu ích để review; real branch-diff review còn thiếu đã được hoàn tất qua PR #25. Trạng thái đóng được chấp nhận: `PASS`.
- Remote branch `docs/ai-ops-gov-002-review-contract-alignment` của PR #25: theo bằng chứng preflight, ref này **vẫn còn tồn tại trên remote** (`git ls-remote --heads origin` trả về ref). Cleanup remote branch **chưa hoàn tất** và là hành động Owner-only; không agent nào tự xóa branch. Branch cleanup không phải là một agent action, và tự nó không phải là permission để bắt đầu product work.
- Tooling migration chưa đóng hoàn toàn vì `AI-OPS-PILOT-001` chưa chạy.
- `AI-OPS-PILOT-001`: `NOT STARTED` — là task kế tiếp.
- `FE-002.1`: `LOCKED` cho đến khi pilot `PASS` và Owner mở task.
- `DATA-001.2`: tiếp tục deferred.

## 2. Today's objective

Chuẩn bị và chạy `AI-OPS-PILOT-001` — một work item nhỏ, hữu ích, không thay đổi product behavior — để chứng minh workflow Claude-write/Codex-review/OC-decide/Owner-merge trên một diff thật. Chỉ sau khi pilot `PASS`, Control Tower mới ra lệnh cấp cao để OC scope-lock `FE-002.1`.

Không mở `DATA-001.2`, payment, tax, discount, production content hoặc capability ngoài booking roadmap hiện hành. Không coi tài liệu sync này (`DOCS-2026-08-09-CT-PACKET`) là implementation diff hoặc evidence của `AI-OPS-PILOT-001`.

## 3. Success criteria

Ngày 2026-08-09 đạt mục tiêu tối thiểu khi:

- Control Tower đã phát lệnh cấp cao cho OC chuẩn bị Master Execution Prompt của `AI-OPS-PILOT-001`;
- OC đã chọn một pilot work item bounded, hữu ích, không đổi product behavior, tách biệt khỏi tài liệu sync này;
- `AI-OPS-PILOT-001` chạy qua đầy đủ Claude implementation → Owner-invoked Codex review → OC decision;
- Codex giữ read-only và review đúng explicit base `origin/develop`;
- `SNAPSHOT.md` được cập nhật bằng evidence pilot sau khi OC/Owner chấp nhận kết quả;
- tooling migration được tuyên bố `PASS` hoặc giữ `BLOCKED` với nguyên nhân cụ thể;
- nếu pilot `PASS`, phạm vi `FE-002.1` được khóa dựa trên source/test hiện tại và có Master Execution Prompt sẵn sàng;
- không có product behavior nào bị thay đổi trong pilot.

Mục tiêu mở rộng, chỉ thực hiện nếu các gate trước đều PASS:

- bắt đầu hoặc hoàn tất implementation `FE-002.1`;
- đưa implementation tới mandatory Codex review;
- không ép merge khi acceptance hoặc review evidence chưa đầy đủ.

## 4. Execution order

1. Control Tower issues lệnh cấp cao cho `AI-OPS-PILOT-001`.
2. OC inspects current evidence và tạo Master Execution Prompt authoritative cho `AI-OPS-PILOT-001`.
3. Chạy `AI-OPS-PILOT-001` với:
   - Claude là sole writer;
   - Codex là read-only reviewer;
   - Owner là sole review-command invoker;
   - OC là người quyết định `PASS`/`CORRECTION_REQUIRED`/`BLOCKED`.
4. Owner accepts hoặc rejects kết quả pilot.
5. Chỉ sau khi pilot `PASS`, Control Tower mới có thể ra lệnh cho OC scope-lock `FE-002.1`.
6. Candidate journey cần khảo sát, chưa phải scope đã duyệt:

   `Create Booking Hold → Confirm Hold → Reservation result`

7. Chỉ sau khi scope lock và Owner authorization, Claude mới được implement `FE-002.1`.
8. Hoàn tất end-of-day worklog, Snapshot synchronization và daily plan tiếp theo dựa trên execution thực tế.

**Correction quan trọng:** Snapshot update do preparatory task này thực hiện không được mô tả như implementation diff của `AI-OPS-PILOT-001`. Control Tower và OC phải chọn một pilot work item riêng, bounded, hữu ích, không đổi product behavior.

Loại trừ bắt buộc khỏi `FE-002.1` trừ khi được duyệt riêng:

- payment hoặc payment status;
- tax, surcharge, discount hoặc currency conversion;
- `DATA-001.2`;
- production content/media;
- Admin Web;
- backend refactor không cần thiết;
- cancellation, trừ khi source evidence chứng minh cancellation là phần bắt buộc của vertical slice đã chọn.

## 5. Hard gates

- `AI-OPS-PILOT-001` phải `PASS` trước khi mở `FE-002.1`.
- `FE-002.1` phải được scope-lock trước implementation.
- `DATA-001.2` vẫn deferred.
- Owner là người duy nhất được invoke Codex review, merge và xóa branch.
- Không agent nào tự bắt đầu task kế tiếp.
- Remote branch cleanup vẫn là Owner-only action và không tự động mở khóa product work.

## 6. Main risks

- Coi tài liệu sync (`DOCS-2026-08-09-CT-PACKET`) là pilot evidence thay vì một work item riêng.
- Tin rằng remote branch đã xóa trong khi ref vẫn tồn tại.
- Biến pilot thành một vòng tooling dài, không tạo evidence mới.
- Tự suy diễn phạm vi `FE-002.1` trước khi đọc source/test.
- Kéo cancellation, payment hoặc data/content work vào vertical slice đầu tiên.
- Automatic Claude–Codex loop làm tăng quota và mất manual gate.
- Bắt đầu product implementation trước khi pilot được OC và Owner chấp nhận.

## 7. Required records

- Daily plan: `docs/daily/2026-08/2026-08-09-plan.md` (file này).
- Pilot evidence/completion report nếu OC yêu cầu lưu lâu dài.
- Daily worklog: `docs/daily/2026-08/2026-08-09-worklog.md`.
- Updated `docs/project/SNAPSHOT.md` sau khi pilot có kết quả.
- Không tạo ADR nếu không có quyết định kiến trúc bền vững mới.

## 8. First action

Control Tower đọc verified startup packet (Snapshot đã sync, PR #25 merged, remote branch state, không có open PR khác) và issue lệnh cấp cao cho OC chuẩn bị Master Execution Prompt cho `AI-OPS-PILOT-001`.
