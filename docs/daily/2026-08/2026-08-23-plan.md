# THE BHA — PLAN 2026-08-23

## 1. Current state

- `ADMIN-002.1` (PMS Reservation Board UI baseline, `Front_End/Admin_Web`):
  `PASS — CLOSED`. PR #32 merged tại
  `17e929d7c1f82941599223344b5f4cdc3aa34307`, `2026-08-22T14:42:31Z`. Bằng
  chứng đầy đủ trong `docs/reports/ADMIN-002.1-completion.md` và
  `docs/daily/2026-08/2026-08-22-worklog.md`.
- Admin Web hiện có một interactive Reservation Board frontend prototype
  chạy trên local deterministic mock state — chưa tích hợp `Back_End/`:
  chưa có database, chưa có auth/RBAC, chưa có OTA, chưa có real
  persistence.
- Work item `ADMIN-002.1-DOCS-CLOSEOUT` (2026-08-22) đã đồng bộ lại các tài
  liệu nguồn sự thật (`AGENTS.md`, `ARCHITECTURE.md`, `PROJECT_BIBLE.md`,
  `SNAPSHOT.md`) theo trạng thái thực tế sau merge PR #32, publish Draft PR
  — xem §7 của `SNAPSHOT.md` cho trạng thái PR chính xác.
- Kiến trúc PMS/database TARGET (Organization/Property scoping,
  multi-RoomType Hold/Reservation, `RoomOccupancySegments`, PostgreSQL
  exclusion constraints, backend Calendar/PMS API, folio persistence,
  auth/RBAC, OTA) vẫn TARGET/APPROVED, chưa implement — không thay đổi bởi
  `ADMIN-002.1` hay docs-closeout.
- `DATA-001.2` tiếp tục dormant/deferred.

## 2. Today's objective

Không mở bất kỳ implementation work item nào trong sản phẩm hôm nay. Trình
tự bị khóa theo đúng thứ tự dưới đây; không được đảo hoặc gộp bước.

## 3. Execution order (locked sequence)

1. Hoàn tất review/merge/branch cleanup cho Draft PR của
   `ADMIN-002.1-DOCS-CLOSEOUT` nếu vẫn còn pending: Owner invoke đúng một
   lượt `/codex:review --base origin/develop`, chuyển kết quả cho OC, OC kết
   luận `PASS`/`CORRECTION_REQUIRED`/`BLOCKED`, và chỉ Owner quyết định
   Ready/merge/branch cleanup.
2. **Chỉ sau khi bước 1 hoàn tất**, bắt đầu một work item riêng biệt để
   cài đặt/đánh giá Graphify cho Claude, theo đúng tooling-adoption gate đã
   có tại `docs/governance/WORKFLOW.md` §12: cài đặt ngoài product source,
   cấu hình bảo toàn Claude-write/Codex-read-only, chạy read-only dry run
   trước, sau đó một pilot nhỏ có thể quan sát được, so sánh với trạng thái
   GitNexus hiện có, và ghi lại rollback/removal step rõ ràng. Work item
   Graphify này cần một Master Execution Prompt riêng của Owner/OC trước
   khi bắt đầu.
3. **Chỉ sau khi** work item Graphify đóng thành công (evidence-based, qua
   đúng gate ở bước 2) **và** Owner đưa ra một quyết định mới, Control
   Tower/OC mới được định nghĩa work item backend Calendar đầu tiên
   (`PMS-DATA-DOCS-001` → implementation). Không có work item backend
   Calendar nào được suy ra hoặc bắt đầu trước điều kiện này.
4. Không bắt đầu backend implementation bên trong work item Graphify ở bước
   2. Không tự suy ra phạm vi chi tiết của phase backend Calendar ở kế
   hoạch này — phạm vi đó sẽ do một Master Execution Prompt riêng, sau
   quyết định của Owner ở bước 3, định nghĩa.

## 4. Hard gates

- Cài đặt/cấu hình Graphify: chỉ được thực hiện trong work item riêng ở
  bước 2, không được thực hiện lẫn vào bất kỳ work item nào khác.
- Bất kỳ implementation backend nào cho PMS TARGET (Organization,
  InventoryHold/Item/Night, ReservationUnit/Night, RoomOccupancySegment,
  RoomBlock, FolioEntries, Stay Declaration, OTA inbox/outbox): **locked**
  cho đến khi cả bước 2 và bước 3 ở §3 hoàn tất.
- Admin Calendar/PMS backend integration, Admin authentication/RBAC thật:
  **locked**.
- `DATA-001.2`: tiếp tục dormant/deferred, không tự động kích hoạt.
- Chỉ Owner được invoke Codex review, merge, và xóa branch.
- Không agent nào tự bắt đầu work item kế tiếp từ việc đóng
  `ADMIN-002.1-DOCS-CLOSEOUT`.

## 5. Main risks

- Coi việc merge `ADMIN-002.1-DOCS-CLOSEOUT` là authorization ngầm cho
  Graphify hoặc backend Calendar work.
- Nhầm frontend mock prototype (`ADMIN-002.1`, PR #32) với backend PMS
  behavior thật khi đọc `PROJECT_BIBLE.md`, `ARCHITECTURE.md`, hoặc
  `SNAPSHOT.md`.
- Bắt đầu work item Graphify mà không có Master Execution Prompt riêng, hoặc
  bỏ qua dry-run/pilot stage của tooling-adoption gate.
- Suy ra phạm vi backend Calendar work item trước khi Graphify đóng và Owner
  ra quyết định mới.

## 6. Required records

Không tạo file tài liệu sản phẩm mới ngoài phạm vi work item Graphify (khi
Owner/OC phát Master Execution Prompt cho nó) cho đến khi có quyết định mới.
Kết quả review/decision của Draft PR docs-closeout được ghi vào worklog
riêng cho ngày review thực tế diễn ra, nếu khác 2026-08-23.

## 7. First action

Xác nhận với Owner trạng thái Draft PR của `ADMIN-002.1-DOCS-CLOSEOUT`
(review/decision đã hoàn tất hay còn pending); nếu còn pending, đó là first
action của ngày trước khi xem xét work item Graphify.
