# THE BHA — PLAN 2026-08-23

## 1. Current state

- `ADMIN-002.1-DOCS-CLOSEOUT`: `PASS — CLOSED`. PR #33 merged tại
  `2c38face7cf51d7271c361e6d684adea466edcf9`, `2026-08-22T15:38:25Z`. Bằng
  chứng đầy đủ trong `docs/daily/2026-08/2026-08-22-worklog.md` §8.
- `TOOL-GRAPHIFY-001` (Graphify tooling-adoption pilot): `PASS — CLOSED`,
  nhưng chỉ dựa trên **replay quản trị do Claude thực hiện**
  (`TOOL-GRAPHIFY-001-DOCS-CLOSEOUT-C4`), không phải trên pilot ban đầu
  Owner tự chạy. Trình tự thật: Owner chạy một pilot sơ bộ local
  (`graphifyy==0.9.48`) → Codex chỉ ra pilot writable do Owner thực hiện vi
  phạm single-writer invariant → Owner chọn Option A (không sửa
  `RULES.md`, thay vào đó ủy quyền replay) → Claude tự replay toàn bộ với
  vai trò writable implementer duy nhất (install/extract/cluster/pilot
  query/verify độc lập với source/probe reuse từ session mới), gate mới
  thật sự đóng. Bằng chứng đầy đủ trong
  `docs/daily/2026-08/2026-08-22-worklog.md` §9-§14 và
  `docs/reports/TOOL-GRAPHIFY-001-completion.md`.
- Work item `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT` (2026-08-22, docs + governance
  replay) đã đồng bộ lại các tài liệu nguồn sự thật (`AGENTS.md`,
  `docs/governance/WORKFLOW.md`, `SNAPSHOT.md`, worklog, report) theo trạng
  thái thực tế sau khi replay C4 hoàn tất, và vẫn đang chờ Owner-invoked
  Codex review + Owner merge decision cho Draft PR #34 — xem `SNAPSHOT.md`
  §7 cho trạng thái PR chính xác. `docs/governance/RULES.md` không đổi
  trong toàn bộ quá trình.
- Admin Web hiện có một interactive Reservation Board frontend prototype
  chạy trên local deterministic mock state — chưa tích hợp `Back_End/`:
  chưa có database, chưa có auth/RBAC, chưa có OTA, chưa có real
  persistence. Không đổi bởi Graphify adoption.
- Kiến trúc PMS/database TARGET (Organization/Property scoping,
  multi-RoomType Hold/Reservation, `RoomOccupancySegments`, PostgreSQL
  exclusion constraints, backend Calendar/PMS API, folio persistence,
  auth/RBAC, OTA) vẫn TARGET/APPROVED, chưa implement — không thay đổi bởi
  Graphify adoption hay bởi bất kỳ docs-closeout nào cho đến nay.
- `DATA-001.2` tiếp tục dormant/deferred.
- Local Graphify tooling state (graph read-only trên máy Claude,
  `graphify-out/` không commit) phải được phân biệt rõ với tracked
  repository state và với product/backend implementation state —
  Graphify không sửa bất kỳ file source nào dưới `Back_End/**` hay
  `Front_End/**`.

## 2. Today's objective

Objective có điều kiện — phụ thuộc trạng thái Draft PR của
`TOOL-GRAPHIFY-001-DOCS-CLOSEOUT`:

- Nếu Draft PR đó chưa được Owner Ready/merge: first action của hôm nay là
  hoàn tất review/merge/branch cleanup cho nó (bước 1 ở §3). Không mở work
  item backend nào trước khi bước này xong.
- Nếu Draft PR đó đã merge trước khi phiên hôm nay bắt đầu: backend
  Calendar/PMS trở thành **intended objective** cho 2026-08-23, với điều
  kiện: Control Tower/OC phải trước tiên định nghĩa work item backend đầu
  tiên và Owner phải phê duyệt một Master Execution Prompt riêng cho nó
  (bước 2-3 ở §3) trước khi Claude được phép implement bất kỳ dòng code
  backend nào. Không có Master Execution Prompt đó nghĩa là chưa có
  implementation nào được phép bắt đầu trong ngày, bất kể objective có điều
  kiện này.

## 3. Execution order (locked sequence)

1. Hoàn tất review/merge/branch cleanup cho Draft PR của
   `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT` nếu vẫn còn pending: Owner invoke đúng
   một lượt `/codex:review --base origin/develop`, chuyển kết quả cho OC,
   OC kết luận `PASS`/`CORRECTION_REQUIRED`/`BLOCKED`, và chỉ Owner quyết
   định Ready/merge/branch cleanup.
2. **Chỉ sau khi bước 1 hoàn tất**, Control Tower/OC định nghĩa work item
   backend Calendar/PMS đầu tiên, dựa trên PMS blueprint đã merge trong PR
   #31 (`docs/design/PMS-DATA-001-core-database-blueprint-v2.md`) và bằng
   chứng backend CURRENT hiện có (single-RoomType Hold/Reservation,
   `BE-003.1`-`BE-003.5`, sáu migration, không đổi).
3. Owner phê duyệt một Master Execution Prompt riêng cho work item backend
   đó — prompt này phải tự định nghĩa phạm vi schema/domain/API/migration
   cụ thể; không được suy luận trước ở đây.
4. **Chỉ sau khi** Master Execution Prompt đó tồn tại **và** Owner kích
   hoạt phiên tương ứng, Claude mới được bắt đầu implementation backend.
   Không có work item backend Calendar nào được suy ra hoặc bắt đầu trước
   điều kiện này.
5. Không bắt đầu backend implementation bên trong work item
   `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT` hay bất kỳ phần còn lại nào của nó.
   Backend code không được đặt trong work item docs này.

## 4. Hard gates

- Cài đặt/cấu hình/nâng cấp/gỡ/rebuild Graphify: không thuộc phạm vi bất kỳ
  work item nào trong kế hoạch hôm nay — pilot đã đóng ở `TOOL-GRAPHIFY-001`
  (§1); mọi thay đổi chính sách Graphify tiếp theo cần một tooling work item
  riêng của Owner.
- Bất kỳ implementation backend nào cho PMS TARGET (Organization,
  InventoryHold/Item/Night, ReservationUnit/Night, RoomOccupancySegment,
  RoomBlock, FolioEntries, Stay Declaration, OTA inbox/outbox): **locked**
  cho đến khi cả bước 2 và bước 3 ở §3 hoàn tất.
- Admin Calendar/PMS backend integration, Admin authentication/RBAC thật:
  **locked**.
- `DATA-001.2`: tiếp tục dormant/deferred, không tự động kích hoạt.
- Chỉ Owner được invoke Codex review, merge, và xóa branch.
- Không agent nào tự bắt đầu work item kế tiếp từ việc đóng
  `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT`.
- Không tự suy ra phạm vi schema/domain/API/migration/authentication/RBAC/
  OTA adapter hay task ID/vertical slice cụ thể cho work item backend đầu
  tiên — phạm vi đó chỉ được định nghĩa bởi Master Execution Prompt riêng ở
  bước 3 của §3.

## 5. Main risks

- Coi việc merge `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT` là authorization ngầm
  cho bất kỳ work item backend cụ thể nào, thay vì chỉ là điều kiện tiên
  quyết đã hoàn tất.
- Nhầm frontend mock prototype (`ADMIN-002.1`, PR #32) với backend PMS
  behavior thật khi đọc `PROJECT_BIBLE.md`, `ARCHITECTURE.md`, hoặc
  `SNAPSHOT.md`.
- Nhầm local Graphify tooling state (đã pilot xong, đọc-only) với một thay
  đổi ở tracked repository state hoặc product/backend implementation
  state.
- Suy ra phạm vi backend Calendar work item (schema/domain/API/migration)
  trước khi Owner phê duyệt Master Execution Prompt riêng cho nó ở bước 3.
- Bắt đầu implementation backend chỉ vì objective hôm nay "có điều kiện" là
  backend — objective có điều kiện không thay thế yêu cầu có Master
  Execution Prompt riêng đã được Owner phê duyệt.

## 6. Required records

Không tạo file tài liệu sản phẩm mới ngoài phạm vi work item backend
Calendar/PMS đầu tiên (khi Owner/OC phát Master Execution Prompt cho nó)
cho đến khi có Master Execution Prompt đó. Kết quả review/decision của
Draft PR `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT` được ghi vào worklog riêng cho
ngày review thực tế diễn ra, nếu khác 2026-08-23.

## 7. First action

Xác nhận với Owner trạng thái Draft PR của `TOOL-GRAPHIFY-001-DOCS-CLOSEOUT`
(review/decision đã hoàn tất hay còn pending); nếu còn pending, đó là first
action của ngày trước khi xem xét work item backend Calendar/PMS.
