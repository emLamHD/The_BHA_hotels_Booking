# THE BHA — PLAN 2026-08-12

## 1. Current state

- Verified baseline sau khi PR #28 (`FE-002.1`) merge: `3f68bd79eff7f6c553e5516431abd09a93298f71`. Đây là baseline đã được xác minh cho tài liệu ngày 2026-08-12, không phải cam kết rằng SHA này sẽ còn là `develop` HEAD sau các commit tài liệu hoặc merge tiếp theo.
- `AI-OPS-PILOT-001` (PR #27, merge `bb64f7e1592f4924049935ecc08922539c532bf8`): `PASS`.
- `FE-002.1` — Hold Confirmation UI (PR #28, published commit `2ccf19b5dba9ac2f71c617420b53a7ddbf7502dd`, merge commit `3f68bd79eff7f6c553e5516431abd09a93298f71`, merged 2026-08-12T13:39:21Z): `PASS — CLOSED`.
- Local và remote feature branch của PR #28 (`feature/fe-002-1-hold-confirmation-ui`): đã xóa (Owner-confirmed).
- Remote branch `docs/ai-ops-gov-002-review-contract-alignment` của PR #25: theo bằng chứng preflight ngày 2026-08-12, ref này **vẫn còn tồn tại trên remote**. Cleanup remote branch vẫn là hành động Owner-only, chưa thực hiện, và không phải là permission để bắt đầu product work.
- Tooling migration: đã hoàn tất — xem `SNAPSHOT.md` §6.
- `DATA-001.2`: tiếp tục deferred/dormant.
- Không có product work item nào đang active tại thời điểm lập plan này.

## 2. Today's objective

Đồng bộ tài liệu vận hành (`SNAPSHOT.md`, plan/worklog ngày hiện tại) với trạng thái thực tế sau khi `FE-002.1` đóng, cộng với một roadmap recommendation cho Control Tower cân nhắc — không phải authorization.

Không mở, không scope-lock và không implement bất kỳ product work item nào (kể cả `DATA-001.2` hay bất kỳ candidate roadmap nào) trong phạm vi ngày này. Không coi việc đóng `FE-002.1` là tự động cấp phép cho work item kế tiếp.

## 3. Success criteria

Ngày 2026-08-12 đạt mục tiêu tối thiểu khi:

- `SNAPSHOT.md` phản ánh đúng baseline `3f68bd79eff7f6c553e5516431abd09a93298f71`, PR #27 và PR #28 đã đóng, và không còn wording circular về việc pilot phải pass trước khi mở `FE-002.1` (vì cả hai đã hoàn tất);
- plan và worklog ngày 2026-08-12 được tạo theo đúng quy ước hiện hành;
- không có product source, test, dependency, configuration hay governance file nào bị thay đổi;
- tài liệu tổng hợp đủ để một Control Tower mới phục hồi trạng thái mà không cần nạp worklog lịch sử;
- tài liệu sync được publish qua một Draft PR riêng, không tự động merge.

## 4. Execution order

1. Đồng bộ `SNAPSHOT.md`, plan và worklog ngày hiện tại dựa trên Git/PR evidence đã xác minh.
2. Publish một Draft documentation PR duy nhất nhắm `develop`.
3. Owner invoke read-only Codex review trên PR tài liệu.
4. OC và Owner quyết định có chấp nhận tài liệu sync hay không.
5. Control Tower và Owner, trong một quyết định riêng biệt với tài liệu sync này, chọn (hoặc tiếp tục hoãn) work item sản phẩm kế tiếp.

Không có task nào tự động bắt đầu sau khi tài liệu sync được merge.

## 5. Hard gates

- `DATA-001.2` vẫn deferred; không tự động kích hoạt.
- Không có work item sản phẩm nào được authorize từ tài liệu sync này.
- Owner là người duy nhất được invoke Codex review, merge và xóa branch.
- Không agent nào tự bắt đầu task kế tiếp.
- Remote branch cleanup (PR #25) vẫn là Owner-only action và không tự động mở khóa product work.

## 6. Main risks

- Baseline bị stale nếu có merge khác xảy ra giữa lúc chuẩn bị và lúc publish tài liệu này.
- Viết lại lịch sử của các plan/worklog trước đó thay vì chỉ tạo file mới.
- Trình bày recommendation roadmap của OC như thể đã được authorize, thay vì để Control Tower/Owner quyết định riêng.
- Copy nhầm wording lịch sử mô tả pilot chưa bắt đầu hoặc `FE-002.1` đang bị khóa vào tài liệu mới.
- Tin rằng remote branch của PR #25 đã xóa trong khi ref vẫn tồn tại.

## 7. Required records

- Daily plan: `docs/daily/2026-08/2026-08-12-plan.md` (file này).
- Daily worklog: `docs/daily/2026-08/2026-08-12-worklog.md`.
- Updated `docs/project/SNAPSHOT.md`.
- Không tạo ADR nếu không có quyết định kiến trúc bền vững mới.
- Không tạo completion report file ngoài phạm vi ba file tài liệu được authorize.

## 8. First action

Xác minh lại `origin/develop` và trạng thái PR #28/PR #27 qua Git/`gh`, sau đó tạo branch tài liệu sync từ baseline đã xác minh.
