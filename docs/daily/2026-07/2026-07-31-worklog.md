# THE BHA — WORKLOG 2026-07-31

## 1. Session objective

Đóng `DATA-001.1`, xác nhận trạng thái repo sau merge và chốt operating model mới trước khi thử Orca cùng các skill chọn lọc từ `mattpocock/skills`.

## 2. `DATA-001.1` closure

Tech Lead review PR #22 với kết quả:

`PASS — recommend Owner merge`

Evidence tại thời điểm review:

- PR #22 open, Draft và mergeable trước khi Owner thao tác;
- base `develop@e5d8b218…`;
- head `3b5b287…`;
- đúng 4 commits với SHA/message dự kiến;
- cumulative diff chỉ có một design document;
- `1737 additions / 0 deletions`;
- CI workflow `success`;
- correction về template-asset rights đúng phạm vi.

Literal `DEFER_DATA-001.2_AND_START_FE-002.1` xuất hiện ba lần trong design document, nhưng chỉ một lần là quyết định hiệu lực tại §18.2; hai lần còn lại là historical checkpoint boundaries. Không yêu cầu correction commit chỉ để đổi cách diễn đạt lịch sử.

Kết luận:

- `DATA-001.1` đạt technical gate;
- recommendation được chấp nhận: `DEFER_DATA-001.2_AND_START_FE-002.1`;
- `DATA-001.2` không triển khai ở thời điểm này.

## 3. Merge and cleanup

Owner đã:

- merge PR #22;
- đưa `develop` tới `a75d27bb3c6b47a9832f022616d94cefbe001d12`;
- xóa feature branch local và remote;
- xác nhận cleanup hoàn tất.

PR #22 không thay đổi product code nên test baseline chức năng vẫn là:

- Frontend `222/222` passing;
- Backend `494/494` passing.

## 4. Workflow correction

Đã đính chính mô hình cũ:

- Control Tower không phải reviewer thường trực sau mọi completion report.
- Executor gửi report cho Owner.
- Owner chuyển report cho OC.
- OC review report/diff/PR trong phiên OC và đưa recommendation.
- Owner là người duy nhất quyết định Ready/merge/cleanup và có chạy task tiếp theo hay không.
- Control Tower được gọi lại cho escalation hoặc để phát lệnh cấp cao tiếp theo.

## 5. New dual-agent decision

The BHA sẽ có cả Claude Code và Codex như executor, nhưng không dùng song song.

Quyết định đã chốt:

- OC tạo một `Master Execution Prompt` dùng chung cho work item.
- `Same prompt = yes; self-divide = no`.
- OC gán từng phase cho Claude hoặc Codex trước khi chạy.
- Hai mode: `SINGLE_AGENT` và `SEQUENTIAL_DUAL_AGENT`.
- Mỗi thời điểm chỉ một Active Executor có quyền ghi vào worktree.
- Dual-agent dùng chung branch/worktree và chuyển giao bằng checkpoint + phase report.
- Agent không gọi lẫn nhau, không nested-agent, không fan-out.
- Task nhỏ ưu tiên single-agent để tránh nhân đôi context/token và handoff cost.

Codex không còn bị cố định là reviewer; Claude không còn bị cố định là executor duy nhất. OC review vẫn độc lập với executor theo governance hiện hành.

## 6. Control Tower context protocol

Khởi động Control Tower mới chỉ nạp mặc định:

1. `RULES.md`
2. `PROJECT_BIBLE.md`
3. `SNAPSHOT.md`
4. plan ngày hiện tại

Output chỉ gồm:

```text
Current state:
Today's objective:
Execution order:
Main risks:
First action:
```

Không kể lại toàn bộ file. Worklog, report, ADR và tài liệu lịch sử chỉ được truy xuất khi có lý do cụ thể.

## 7. Tooling decisions

### GitNexus

- Tiếp tục là code graph/impact analysis hiện hành.
- Không thay thế source, tests hoặc project sources of truth.

### Orca

- Sẽ thử ở vai trò cockpit/worktree manager.
- Không cho phép parallel execution, auto-routing hoặc nested orchestration trong pilot.
- Không cấp quyền merge và không dùng metadata Orca thay Git/Snapshot.

### `mattpocock/skills`

- Không cài toàn bộ repository.
- Ưu tiên review/adapt `diagnosing-bugs`.
- `grilling`, TDD và review skill chỉ lấy nguyên tắc khi có use case.
- Không chạy setup toàn cục trước khi review side effects.

## 8. Documentation migration

Bộ tài liệu cần cập nhật trước install/config:

- `docs/governance/RULES.md`;
- `docs/governance/WORKFLOW.md`;
- `docs/project/PROJECT_BIBLE.md` với clarification về asset rights;
- `docs/project/SNAPSHOT.md`;
- plan/worklog hiện tại;
- root `AGENTS.md` làm shared executor contract;
- root `CLAUDE.md` làm Claude adapter, không còn tuyên bố “Claude replaced Codex”.

Trạng thái cuối phiên: documentation package đã được chuẩn bị; chờ Owner áp dụng vào repo và xác nhận trước khi chuyển sang `install → config → dry run/pilot`.

## 9. Product work lock

- `FE-002.1` là product work item tiếp theo theo recommendation đã chấp nhận.
- Chưa mở execution `FE-002.1` trong tooling migration.
- `DATA-001.2` vẫn deferred.
- Sau khi pilot pass, Control Tower mới phát lệnh cấp cao cho OC lập workflow `FE-002.1`.

## 10. Next action

Owner áp dụng bộ Markdown, xác nhận `develop` HEAD, `git status` và mọi chỉnh sửa thủ công. Sau xác nhận mới bắt đầu cài/config Orca và skill chọn lọc.
